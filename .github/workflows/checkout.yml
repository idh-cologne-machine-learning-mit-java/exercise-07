name: Checkout
on: push
jobs:
  checkout:
    name: tests
    runs-on: ubuntu-latest
    steps:
      - name: Java setup
        uses: actions/setup-java@v1
        with:
          java-version: '14.0.2'
      - run: mvn test
